import React from 'react';

const logoStyle = {
    height: "150px",
    width: "100%"
}

export default class Logo extends React.Component {

    render() {
        return (
            <div style={ logoStyle }>
            </div>
        )
    }
}